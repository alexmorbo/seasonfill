apiVersion: v2
name: seasonfill-helm
description: |
  Seasonfill — a multi-instance Sonarr backfill controller that scans
  managed series for incomplete seasons and triggers indexer searches
  on a schedule. Singleton service; database stores state. Frontend
  (React SPA served by nginx) ships as a second Deployment.
type: application
version: 0.6.1
appVersion: "0.6.1"
kubeVersion: ">=1.25.0-0"
maintainers:
  - name: AlexMorbo
    email: alex@morbo.ru
sources:
  - https://github.com/alexmorbo/seasonfill
home: https://github.com/alexmorbo/seasonfill
icon: https://raw.githubusercontent.com/alexmorbo/seasonfill/main/web/src/assets/logo.svg
keywords:
  - sonarr
  - media
  - automation
  - backfill
annotations:
  artifacthub.io/license: GPL-3.0-or-later
  artifacthub.io/category: networking
  artifacthub.io/images: |
    - name: seasonfill
      image: ghcr.io/alexmorbo/seasonfill:0.6.1
    - name: seasonfill-web
      image: ghcr.io/alexmorbo/seasonfill-web:0.6.1
  artifacthub.io/changes: |
    - {kind: fixed, description: "web image now bakes the real release version (e.g. 0.6.1) into VITE_APP_VERSION instead of the raw 40-char git SHA; the TopBar in the SPA now shows v0.6.1 instead of a hex hash"}
